{
  "_설명": "이 블록을 공유 Firebase 프로젝트의 기존 database rules 안에 병합한다. <APP_ID>를 app-config.js의 값으로 치환. 기존 다른 게임의 규칙을 덮어쓰지 말 것. 'read/write: true' 전체 개방은 금지 (누구나 데이터 삭제 가능).",
  "rules": {
    "<APP_ID>_rooms": {
      "$code": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "$code.matches(/^[A-HJ-NP-Z2-9]{4,8}$/)"
      }
    }
  }
}
